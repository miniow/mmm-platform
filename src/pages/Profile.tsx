import React from 'react';

const Profile: React.FC = () => {
    return (
        <div>
            <h1>Profile</h1>
            <p>This is your profile page. Only authenticated users can see this.</p>
        </div>
    );
};

export default Profile;